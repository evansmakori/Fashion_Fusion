{
  "name": "fashion-fusion-shop",
  "version": "1.0.0",
  "private": true,
  "description": "LiquidMetal AI app",
  "scripts": {
    "prebuild": "npm --prefix src/frontend-service ci && npm --prefix src/frontend-service run build && node src/frontend-service/build_embed_assets.mjs",
    "build": "shx rm -rf dist && tsc -b",
    "start": "raindrop build deploy --start",
    "stop": "raindrop build stop",
    "restart": "raindrop build deploy --start",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@liquidmetal-ai/raindrop-framework": "0.12.0",
    "@modelcontextprotocol/sdk": "^1",
    "hono": "^4",
    "kysely": "^0.27.2",
    "kysely-d1": "^0.3.0",
    "zod": "^3"
  },
  "devDependencies": {
    "firebase-admin": "^13.6.0",
    "shx": "^0.4.0",
    "ts-node": "^10",
    "typescript": "^5.0.4",
    "typescript-eslint": "^8.7.0",
    "vitest": "^3.1.3"
  }
}
